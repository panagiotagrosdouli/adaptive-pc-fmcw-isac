# Manifests

Repository-level home for frozen code, dataset, split, experiment, model, and configuration manifests. Large raw datasets and checkpoints are not committed; their identities and hashes are.
