# Stage 04 — Frozen Predictor Release

This stage freezes the exact predictor checkpoint and preprocessing identity
before Stage 05 touches official validation data. It remains `BLOCKED` until a
real Stage-03 training run produces a selected checkpoint.
