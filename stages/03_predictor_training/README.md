# Stage 03 — Predictor Training Contract

This stage validates the immutable inputs to predictor training. It does not
train a model or claim predictor performance. Training and development scenario
identities must be disjoint, and official validation must remain unavailable to
model selection. The stage remains `BLOCKED` until real Stage-01 corpora exist.
